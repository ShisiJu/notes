/**
 * @param {string} s
 * @return {number}
 */
var calculate = function (s) {
  let expr = s.replace(' ', '')
  

  let opt = []
  let num = []

  for (let i = 0; i < expr.length; i++) {
    if (isNumber()) {

    } else {

    }

  }

};